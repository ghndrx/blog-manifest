apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-service
  name: nginx-service
  namespace: blog
spec:
  ports:
    - name: http-redirect
      port: 80
    - name: ghost-https
      port: 443
  selector:
    app: nginx-proxy
  type: LoadBalancer