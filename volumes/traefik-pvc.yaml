apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-pvc
  labels:
    app: traefik
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
